# JWT Signing Secret (Change to a complex random key in production)
JWT_SECRET=mc-hosting-dev-secret-change-in-production

# Port to bind the Express API server
API_PORT=3001

# WebSocket Relay Configuration
RELAY_HOST=relay.mchosting.local
RELAY_PORT=8443
